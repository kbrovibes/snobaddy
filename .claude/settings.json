{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git pull --rebase --autostash 2>&1 || { git rebase --abort 2>/dev/null; echo '{\"systemMessage\":\"git pull --rebase failed — conflicts detected. Please pull and resolve manually before continuing.\"}'; }",
            "statusMessage": "Syncing with remote..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "git pull --rebase --autostash 2>&1 || { git rebase --abort 2>/dev/null; echo '{\"systemMessage\":\"git pull --rebase failed before push — conflicts detected. Resolve manually before pushing.\"}'; }",
            "statusMessage": "Pulling latest before push..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash scripts/changelog-hook.sh"
          }
        ]
      }
    ]
  }
}
