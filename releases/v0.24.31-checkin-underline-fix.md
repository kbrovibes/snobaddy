# Release v0.24.31 — Remove underline from Check In/Out buttons

## Summary
Removed explicit `underline` class from Check In / Check Out buttons in PlayerCheckinCard. Also corrected color from `text-blue-600` to `text-sky-600` / `text-red-600` to match the Header logout button style.

## Changes
| File | Action |
|------|--------|
| `src/components/PlayerCheckinCard.tsx` | Removed `underline underline-offset-2`, changed `text-blue-600` → `text-sky-600` (Check in) / `text-red-600` (Check out), added `font-medium` |
