# v0.32.0 — Season Lifecycle Management

**Date:** 2026-05-01
**Spec:** [34 — Season Lifecycle](../specs/34-season-lifecycle.md)

## Summary

Adds season lifecycle state machine (active/upcoming/completed) with DB migration, seasons DB layer, and season-aware creation routes. This is the foundation — UI and season-scoped queries coming in subsequent commits.

## Changes

### New Files
| File | Purpose |
|------|---------|
| `supabase/migrations/20260501_season_status.sql` | Adds `status` column to `seasons` table + partial unique index |
| `src/lib/db/seasons.ts` | Season CRUD: getAllSeasons, getActiveSeason, createSeason, updateSeasonStatus |

### Modified Files
| File | Change |
|------|--------|
| `src/app/api/sessions/create/route.ts` | Use `status='active'` instead of `ORDER BY start_date DESC` |
| `src/app/api/sessions/start-today/route.ts` | Same active-season query change |
| `src/app/api/finals/route.ts` | Same active-season query change |

## Migration

Run in Supabase SQL editor:
```sql
ALTER TABLE seasons ADD COLUMN status TEXT NOT NULL DEFAULT 'active'
  CHECK (status IN ('active', 'upcoming', 'completed'));

CREATE UNIQUE INDEX idx_seasons_one_active
  ON seasons ((1)) WHERE status = 'active';
```

The existing season row will default to `active`.
