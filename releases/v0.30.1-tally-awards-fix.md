# v0.30.1 — Tally session awards fix

**Date:** 2026-04-24

## Summary
Tally/whiteboard sessions no longer show the 5-card `SessionHighlights` component (which includes point-based and margin-based awards that don't apply to tally mode). Only the 3-card `TallyHighlights` is shown.

## Files changed
| File | Change |
|------|--------|
| `src/app/(app)/session/[id]/page.tsx` | Added `tallyRows.length === 0` guard to `SessionHighlights` render condition |
