# v0.27.1 — Admin-only Leaderboard

## Summary
Restrict Leaderboard visibility to admins.

## Changes
- `src/components/BottomNav.tsx` — Set `adminOnly: true` on the Leaderboard nav item so it's filtered out for non-admins.
- `src/app/(app)/leaderboard/page.tsx` — Server-side guard: redirect to `/` when the current player is not an admin.

## Notes
- No DB changes.
- `BottomNav` already received `isAdmin` from `(app)/layout.tsx` and filtered `adminOnly` items; this change just flips the flag.
