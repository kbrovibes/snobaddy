# Release v0.24.30 — Check In/Out button style

## Summary
Restyled Check In / Check Out buttons in the Players list to match the Logout button in the Header.

## Changes
| File | Action |
|------|--------|
| `src/components/AdminPresenceToggle.tsx` | Removed pill/border styling; now plain text links (`text-sky-600` / `text-red-600`) matching the Header logout button |
