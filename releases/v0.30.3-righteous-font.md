# v0.30.3 — Righteous font for header logo

## Changes
- `src/app/layout.tsx` — imported `Righteous` from `next/font/google`, added `--font-righteous` CSS variable
- `src/components/Header.tsx` — applied `var(--font-righteous)` to "Serve Sports" span; reverted second size bump back to `text-2xl` / `text-[13px]`
