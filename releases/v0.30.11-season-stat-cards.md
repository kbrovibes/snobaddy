# v0.30.11 — Season stat cards on home page

## Changes
- `src/lib/db/sessions.ts` — added `getSeasonPlayerCount(sessionIds)` — counts distinct players across given sessions via session_players
- `src/app/(app)/page.tsx` — computes daysOfPlay, totalMatches, playerCount from non-test completed sessions; renders 3 stat cards (🏸 Players, 🎯 Matches, 📅 Days of Play) above the session list when at least one session is completed
