# v0.33.2 — Leaderboard Runner-Ups

## Summary
Added runner-up cards below the Badminton Nut and Nut Cracker award winners on the leaderboard page.

## Files Changed
| File | Action |
|---|---|
| `src/app/(app)/leaderboard/LeaderboardTable.tsx` | Modified — added `RunnerUpCard` component, extended top-3 computation for both awards, added second row of runner-up cards |

## Changes

### LeaderboardTable.tsx
- Added `RunnerUpCard` component: compact card showing 🥈/🥉 runners-up with name and stat, using same card aesthetic as `AwardCard`
- Replaced `badmintonNut` and `nutCracker` single-player reduce logic with top-3 sorted arrays (`badmintonTop3`, `nutTop3`)
- Derived `badmintonRunners` and `nutRunners` (2nd and 3rd place) from those arrays
- Added a second `grid grid-cols-2` row beneath the winner cards, only rendered when runners exist
