# Release v0.9.4 — Refined Naming & Layout Alignment

## Overview
This release corrects several issues from the previous version, specifically regarding player name display, match layout alignment, and a database data correction.

## Changes

### 1. Refined Naming Logic
- **Scope:** "Who's Here" list and Scoreboard.
- **Action:** Reverted to showing **full names** for all players in these sections to ensure clarity.
- **Scope:** Match History ("Matches" table).
- **Action:** Maintained **first names only** for players in this specific section to keep the match rows clean and readable on mobile.

### 2. Match History Layout Alignment
- **Component:** `src/app/(app)/page.tsx`
- **Action:** Changed the alignment of Team 1 (the first column in the match grid) from `text-right` to `text-left`.
- **Reasoning:** Improving readability by ensuring both team columns follow a standard left-to-right reading pattern within their respective grid cells.

### 3. Database Data Correction (Attempt 2)
- **Target:** `chinnu.n.chunni@gmail.com` (corrected from previously used incorrect email address).
- **Action:** Updated name to "Vinaya Krishnan".
- **Verification:** Successfully executed via service role script after locating the correct record.

## Technical Notes
- Component state was carefully audited to ensure the `getFirstName` helper is only used in the intended JSX blocks.
- Grid column definition `grid-cols-[1fr_auto_1fr]` remains to keep the "vs" separator perfectly centered while both team columns are now left-aligned within their allocated space.
