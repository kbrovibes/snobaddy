# v0.28.2 — Fun Names Fix

## Summary
Fun names toggle in Finals now works across all modes (fixed-partner and playoffs), and the blending algorithm uses 2-3 chars from each player name.

## Changes

| File | Change |
|------|--------|
| `src/components/finals/FinalsGroupView.tsx` | Always pass `playerNames` to `FinalsMatchList` (was `undefined` for fixed-partner) |
| `src/components/finals/FinalsMatchList.tsx` | `getTeamLabel`: generate fun name for configured pairs when toggle is on |
| `src/components/finals/FinalsMatchList.tsx` | Rewrite `generateFunName` to guarantee 2+ chars from each name, prefer 3 at syllable breaks |
