# v0.30.4 — Photo upload enabled for all admins

## Changes
- `src/app/(app)/session/[id]/page.tsx` — both `TallyEntryForm` call sites changed from `isGodMode={isGodMode}` to `isGodMode={isAdmin}`, making the 📷 photo import button visible to any admin
