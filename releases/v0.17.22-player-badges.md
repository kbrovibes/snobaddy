# v0.17.22 — Player badge redesign

## What changed
- New shared `PlayerBadges.tsx` component with `VerifiedBadge` and `AdminBadge`
- **VerifiedBadge**: inline SVG — blue filled circle (#3B82F6) with white checkmark path (Instagram-style)
- **AdminBadge**: 🛡️ emoji with `title="Admin"`
- Order: name → VerifiedBadge → AdminBadge

## Files modified
| File | Change |
|---|---|
| `src/components/PlayerBadges.tsx` | New — shared badge components |
| `src/app/(app)/leaderboard/LeaderboardTable.tsx` | Use VerifiedBadge + AdminBadge |
| `src/components/PlayerCheckinCard.tsx` | Use VerifiedBadge + AdminBadge |
