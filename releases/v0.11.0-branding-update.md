# Release v0.11.0: Branding Update

## Technical Detail
- Changed site title to "SnoBaddy Dashboard" in the root layout metadata.
- Updated favicon to use `/serve-logo.jpg` via metadata `icons` property.
- Removed default `src/app/favicon.ico` to prevent it from overriding the metadata icon.

## Files Changed
| File | Action |
|---|---|
| `src/app/layout.tsx` | Modified metadata title and icons. |
| `src/app/favicon.ico` | Deleted default icon. |
