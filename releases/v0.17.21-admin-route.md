# v0.17.21 — Admin Panel moved to /admin

## What changed
- Admin Panel page moved from `/players` → `/admin`
- Non-admin users hitting `/admin` are `redirect("/")` back to the session page
- BottomNav href updated from `/players` to `/admin`
- Old `src/app/(app)/players/page.tsx` deleted

## Files modified
| File | Change |
|---|---|
| `src/app/(app)/admin/page.tsx` | New — admin panel with non-admin redirect guard |
| `src/app/(app)/players/page.tsx` | Deleted |
| `src/components/BottomNav.tsx` | href `/players` → `/admin` |

## Notes
- Individual player profiles at `/players/[id]` are unaffected.
- API routes under `/api/players/` are unaffected.
